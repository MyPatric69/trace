# Generated by: agentguard init --guided
# Date: 2026-09-05
# Concretization: AI-assisted (anthropic/claude-haiku-4-5-20251001)
# Review with: agentguard check --ai-review

owner: "Patric"

scope:
  authorized:
    - action: "mplement features in engine/, dashboard/, hooks/, server/, tests/. Read and write trace_config.yaml, ~/.trace/user_config.yaml, ~/.claude/settings.json, ~/.claude/statusline_bridge.sh. Run pytest tests/ -v. Execute git add/commit/push/pull. Manage LaunchAgents via launchctl. One external API call permitted: api.anthropic.com/v1/messages/count_tokens. No direct access to ~/.trace/trace.db."
      reason: "Extracted from governance definition — review and refine"
      added: "2026-09-05"
  prohibited:
    - action: "Never delete or modify ~/.trace/trace.db directly. Never push to main without explicit user approval. Never execute git reset --hard or any history-rewriting command without explicit user confirmation. Never call external APIs other than api.anthropic.com/v1/messages/count_tokens. Never modify files outside the project directory, ~/.trace/, or ~/.claude/. Never commit in a loop – maximum one chore(context): auto-sync commit per session end. Never install system packages without explicit user confirmation."
      reason: "Extracted from governance definition — review and refine"
      severity: "HARD_LIMIT"
      added: "2026-09-05"
  requires_confirmation:
    []

escalation:
  contact: "patric@hayna.net"
  method: "log"
  trigger: "2+ critical failures or loop detected"

killswitch: "AgentGuard stops it automatically"

path_policy:
  denied_paths:
    - pattern: ".env*"
      reason: "Secrets must never be written by the agent"
    - pattern: ".git/**"
      reason: "Git internals must not be modified directly"
  protected_paths: []
  authorized_paths:
    - pattern: ".claude/**"
      reason: "Auto-detected project directory"
    - pattern: ".pytest_cache/**"
      reason: "Auto-detected project directory"
    - pattern: ".ruff_cache/**"
      reason: "Auto-detected project directory"
    - pattern: ".vscode/**"
      reason: "Auto-detected project directory"
    - pattern: "dashboard/**"
      reason: "Auto-detected project directory"
    - pattern: "docs/**"
      reason: "Auto-detected project directory"
    - pattern: "engine/**"
      reason: "Auto-detected project directory"
    - pattern: "hooks/**"
      reason: "Auto-detected project directory"
    - pattern: "server/**"
      reason: "Auto-detected project directory"
    - pattern: "tests/**"
      reason: "Auto-detected project directory"
    - pattern: "*.md"
      reason: "Documentation"
  default_for_unmatched: "ask"

cost_awareness:
  thresholds:
    - at_usd: 5.0
      level: warn
  repeat_last_threshold: true
  repeat_interval_usd: 10.0
governance_history:
  - date: "2026-09-05"
    action: "Initial governance created"
    tool: "agentguard init --guided"
    version: "1.0.8"
